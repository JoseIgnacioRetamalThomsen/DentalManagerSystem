<Page
    x:Class="DentalManagerSys.Views.ViewAllCustomers"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DentalManagerSys.Views"
     xmlns:local1="using:Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>


        <ListView x:ConnectionId='2' x:Name="CustomersListView" Height="200"
                      ScrollViewer.HorizontalScrollMode="Enabled" 
                       ScrollViewer.HorizontalScrollBarVisibility="Auto"
                      ScrollViewer.VerticalScrollMode="Enabled"
                       ScrollViewer.VerticalScrollBarVisibility="Auto"
                          SelectionMode="Single"
                          IsItemClickEnabled="True">
            <!--ItemClick="AccountsListView_ItemClick"
                          SelectionChanged="AccountsListView_SelectionChanged"-->

            <ListView.ItemTemplate>
                <DataTemplate                              >
                    <Grid x:ConnectionId='3' >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150"/>
                            <ColumnDefinition Width="350" />
                            <ColumnDefinition  Width="150"/>

                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock x:ConnectionId='4'                    Grid.Column="0" Grid.Row="0"/>
                        <TextBlock x:ConnectionId='5'                         Grid.Column="1" Grid.Row="0"/>
                        <TextBlock x:ConnectionId='6'                      Grid.Column="2" Grid.Row="0"/>

                    </Grid>
                </DataTemplate>

            </ListView.ItemTemplate>
        </ListView>

    </Grid>
</Page>

