<Page
    x:Class="DentalManagerSys.Views.EditTreatment"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:DentalManagerSys.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <StackPanel x:Name="TreatmentName" Grid.Row="0" Grid.Column="0" Margin="5">
            <TextBlock x:Uid="/Strings/ViewTreatmentName"/>
            <TextBox x:Name="TreatmentNameTextBox"/>
        </StackPanel>

        <StackPanel x:Name="TreatmentPrice" Grid.Row="0" Grid.Column="1" Margin="5">
            <TextBlock x:Uid="/Strings/ViewTreatmentPrice"/>
            <TextBox x:Name="TreatmentPriceTextBox"/>
        </StackPanel>

    </Grid>
</Page>
